# 2026-06-18

- After the last change, OpenClaw was not accessible via the browser.
- The recovery was to bring OpenClaw online again, which restored browser access.
- The Windows desktop `OpenClaw Control` shortcut was updated to probe `openclaw dashboard --yes --no-open` at launch and use the live dashboard URL instead of a hardcoded token fragment.
- The desktop shortcut now points at `C:\Users\ralsu\.openclaw-control\openclaw-control-launch.ps1` and launches the browser with a dedicated `OCBrowserProfile` and remote debugging on port `9222`.
- The desktop shortcut now also opens `http://127.0.0.1:8792/spline-lab.html` automatically, and it best-effort starts a local Playwright MCP server on port `8931` attached to that Chrome session.
- `BOOT.md` now notes that Spline Lab should be started automatically on OC gateway startup via `run_vector_viewer.sh`.
- The control launcher now writes a simple status file at `/tmp/openclaw-control-status.txt` so the browser session can show whether Playwright MCP attached or whether the browser debug port timed out.
- The OpenClaw Control shortcut now opens a local `openclaw-control.html` tab first, with a visible status banner plus links to the dashboard and Spline Lab, and then opens the live tabs behind it.
- Verified by raw CDP attach that the live `Spline-Lab` tab returns actual DOM text: `document.title` is `Spline-Lab`, the page `h1` is `Spline-Lab`, and the body text includes the live solver/readout content.
- The launch status banner was moved into `spline-lab.html` itself via a local `/api/openclaw-status` endpoint, and the Windows launcher no longer needs to open a separate `openclaw-control.html` page for status.
- The launch banner now sits inline to the right of the `Spline-Lab` heading, and the live Spline Lab server was restarted so `/api/openclaw-status` now returns the OpenClaw launch message instead of 404.
